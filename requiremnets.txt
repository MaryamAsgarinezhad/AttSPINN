numpy>=1.21.0
pandas>=1.3.0
scikit-learn>=1.0.0   # for train_test_split, metrics

torch>=1.13.0         # for all torch.*, nn, autograd, optim, etc.
tqdm>=4.60.0          # for progress bars in training loop

